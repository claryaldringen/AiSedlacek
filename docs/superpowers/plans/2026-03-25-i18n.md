# i18n (Vícejazyčnost) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Přidat vícejazyčnost (čeština + angličtina, rozšiřitelné) do celé aplikace — UI, API, emaily.

**Architecture:** next-intl s URL prefix routingem (`/en/`, `/cs/`). Přesun App Routeru pod `[locale]/` segment. Middleware kombinuje locale detection + auth. Překladové soubory v `messages/`. User preference v DB propagovaná přes `NEXT_LOCALE` cookie.

**Tech Stack:** next-intl, Next.js 15 App Router, Prisma

**Spec:** `docs/superpowers/specs/2026-03-25-i18n-design.md`

---

## File Map

| Action | File | Účel |
|--------|------|------|
| Create | `apps/web/i18n/routing.ts` | Locale routing config |
| Create | `apps/web/i18n/request.ts` | Request config pro next-intl |
| Create | `apps/web/i18n/navigation.ts` | Lokalizované Link, redirect, usePathname |
| Create | `apps/web/messages/en.json` | Anglické překlady |
| Create | `apps/web/messages/cs.json` | České překlady |
| Modify | `apps/web/next.config.ts` | next-intl plugin |
| Modify | `apps/web/package.json` | Přidat next-intl dependency |
| Rewrite | `apps/web/middleware.ts` | Locale detection + auth |
| Rewrite | `apps/web/app/layout.tsx` | Minimální root layout (html/body) |
| Create | `apps/web/app/[locale]/layout.tsx` | Locale-aware layout s providers |
| Move | `apps/web/app/(public)/` → `apps/web/app/[locale]/(public)/` | Landing page |
| Move | `apps/web/app/login/` → `apps/web/app/[locale]/login/` | Login |
| Move | `apps/web/app/forgot-password/` → `apps/web/app/[locale]/forgot-password/` | Forgot password |
| Move | `apps/web/app/reset-password/` → `apps/web/app/[locale]/reset-password/` | Reset password |
| Move | `apps/web/app/verify-email/` → `apps/web/app/[locale]/verify-email/` | Verify email |
| Move | `apps/web/app/view/` → `apps/web/app/[locale]/view/` | Public view |
| Move | `apps/web/app/workspace/` → `apps/web/app/[locale]/workspace/` | Workspace |
| Move | `apps/web/app/not-found.tsx` → `apps/web/app/[locale]/not-found.tsx` | Lokalizovaný 404 |
| Move | `apps/web/app/error.tsx` → `apps/web/app/[locale]/error.tsx` | Lokalizovaný error |
| Create | `apps/web/app/not-found.tsx` | Root 404 (anglický fallback) |
| Modify | 19 komponent v `apps/web/components/` | Nahradit hardcoded texty za `t()` |
| Modify | ~20 API routes v `apps/web/app/api/` | Lokalizované chybové hlášky |
| Create | `apps/web/lib/infrastructure/api-locale.ts` | Helper pro locale z API requestu |
| Modify | `packages/shared/src/domain/email.ts` | IEmailProvider +locale param |
| Modify | `apps/web/lib/adapters/email/resend-email.ts` | Locale-aware šablony |
| Modify | `apps/web/lib/adapters/email/console-email.ts` | Locale param |
| Modify | `apps/web/prisma/schema.prisma` | User.locale pole |
| Create | `apps/web/app/api/user/locale/route.ts` | PATCH endpoint pro locale |
| Create | `apps/web/components/LocaleSwitcher.tsx` | Přepínač jazyka |
| Modify | `apps/web/lib/auth.ts` | NEXT_LOCALE cookie při přihlášení |

---

### Task 1: Instalace next-intl a základní konfigurace

**Files:**
- Modify: `apps/web/package.json`
- Create: `apps/web/i18n/routing.ts`
- Create: `apps/web/i18n/request.ts`
- Create: `apps/web/i18n/navigation.ts`
- Modify: `apps/web/next.config.ts`

- [ ] **Step 1: Nainstalovat next-intl**

```bash
cd apps/web && npm install next-intl
```

- [ ] **Step 2: Vytvořit routing config**

`apps/web/i18n/routing.ts`:

```typescript
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'cs'],
  defaultLocale: 'en',
});
```

- [ ] **Step 3: Vytvořit request config**

`apps/web/i18n/request.ts`:

```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  if (!locale || !routing.locales.includes(locale as (typeof routing.locales)[number])) {
    locale = routing.defaultLocale;
  }
  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 4: Vytvořit navigation helpers**

`apps/web/i18n/navigation.ts`:

```typescript
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
```

- [ ] **Step 5: Upravit next.config.ts**

`apps/web/next.config.ts`:

```typescript
import type { NextConfig } from 'next';
import { execSync } from 'child_process';
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

const gitHash = (() => {
  try {
    return execSync('git rev-parse --short HEAD').toString().trim();
  } catch {
    return 'unknown';
  }
})();

const nextConfig: NextConfig = {
  transpilePackages: ['@ai-sedlacek/shared'],
  serverExternalPackages: ['sharp', 'tesseract.js'],
  env: {
    NEXT_PUBLIC_BUILD_TIME: new Date().toISOString(),
    NEXT_PUBLIC_BUILD_HASH: gitHash,
  },
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 6: Typecheck**

Run: `npx turbo typecheck --force`

- [ ] **Step 7: Commit**

```bash
git add apps/web/package.json apps/web/package-lock.json apps/web/i18n/ apps/web/next.config.ts
git commit -m "feat: next-intl instalace a základní konfigurace"
```

---

### Task 2: Překladové soubory

**Files:**
- Create: `apps/web/messages/en.json`
- Create: `apps/web/messages/cs.json`

- [ ] **Step 1: Vytvořit en.json**

Vytvořit `apps/web/messages/en.json` s kompletním překladovým stromem. Klíče jsou organizované podle domény. Projít VŠECHNY komponenty a stránky a extrahovat texty. Struktura:

```json
{
  "metadata": { ... },
  "common": { ... },
  "auth": { ... },
  "workspace": { ... },
  "document": { ... },
  "collection": { ... },
  "billing": { ... },
  "errors": { ... },
  "emails": { ... },
  "landing": { ... },
  "view": { ... }
}
```

Pro přesné klíče: projít každý soubor v `components/` a `app/`, najít všechny hardcoded české texty, vytvořit odpovídající anglický klíč. Pojmenování klíčů: camelCase, popisné (např. `auth.emailNotVerified`, `workspace.sortByName`, `document.noTranscription`).

Parametrizované texty používají ICU syntax: `{count, plural, one {# stránka} few {# stránky} other {# stránek}}` → `{count, plural, one {# page} other {# pages}}`.

- [ ] **Step 2: Vytvořit cs.json**

Stejná struktura jako en.json, hodnoty převzaté z existujícího hardcoded kódu. Toto je přímá extrakce — žádné překlady, jen kopie existujících českých textů do JSON.

- [ ] **Step 3: Commit**

```bash
git add apps/web/messages/
git commit -m "feat: překladové soubory en.json + cs.json"
```

---

### Task 3: Přesun App Router pod [locale]/ a layout split

**Files:**
- Rewrite: `apps/web/app/layout.tsx`
- Create: `apps/web/app/[locale]/layout.tsx`
- Move: všechny page adresáře pod `[locale]/`
- Create: `apps/web/app/not-found.tsx` (nový root)

Toto je nejkritičtější task — strukturální změna celého App Routeru.

- [ ] **Step 1: Vytvořit [locale] adresář a přesunout stránky**

```bash
cd apps/web/app
mkdir -p '[locale]'
mv '(public)' '[locale]/'
mv login '[locale]/'
mv forgot-password '[locale]/'
mv reset-password '[locale]/'
mv verify-email '[locale]/'
mv view '[locale]/'
mv workspace '[locale]/'
```

- [ ] **Step 2: Přesunout error.tsx a not-found.tsx pod [locale]/**

```bash
mv error.tsx '[locale]/'
mv not-found.tsx '[locale]/not-found.tsx'
```

- [ ] **Step 3: Přepsat root layout.tsx**

`apps/web/app/layout.tsx` — minimální root layout bez locale:

```tsx
import { SessionProvider } from 'next-auth/react';
import './globals.css';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}): React.ReactElement {
  return children as React.ReactElement;
}
```

Poznámka: next-intl doporučuje, aby root layout NEOBSAHOVAL `<html>` tag — ten bude v `[locale]/layout.tsx`.

- [ ] **Step 4: Vytvořit [locale]/layout.tsx**

`apps/web/app/[locale]/layout.tsx`:

```tsx
import type { Metadata } from 'next';
import { NextIntlClientProvider } from 'next-intl';
import { getMessages, getTranslations } from 'next-intl/server';
import { notFound } from 'next/navigation';
import { SessionProvider } from 'next-auth/react';
import { BuildInfo } from '@/components/BuildInfo';
import { routing } from '@/i18n/routing';

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'metadata' });
  return {
    title: { default: t('title'), template: '%s — AiSedlacek' },
    description: t('description'),
    openGraph: {
      type: 'website',
      locale: locale === 'cs' ? 'cs_CZ' : 'en_US',
      siteName: 'AiSedlacek',
      title: t('title'),
      description: t('description'),
    },
    twitter: {
      card: 'summary_large_image',
      title: t('title'),
      description: t('description'),
    },
    metadataBase: new URL(process.env.NEXTAUTH_URL ?? 'https://aisedlacek.cz'),
  };
}

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({ children, params }: Props): Promise<React.ReactElement> {
  const { locale } = await params;

  if (!routing.locales.includes(locale as (typeof routing.locales)[number])) {
    notFound();
  }

  const messages = await getMessages();

  return (
    <html lang={locale}>
      <body className="bg-slate-800 text-slate-900">
        <SessionProvider>
          <NextIntlClientProvider messages={messages}>
            {children}
            <BuildInfo />
          </NextIntlClientProvider>
        </SessionProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 5: Vytvořit root not-found.tsx (anglický fallback)**

`apps/web/app/not-found.tsx`:

```tsx
export default function RootNotFound(): React.ReactElement {
  return (
    <html lang="en">
      <body>
        <div style={{ display: 'flex', minHeight: '100vh', alignItems: 'center', justifyContent: 'center', fontFamily: 'serif' }}>
          <div style={{ textAlign: 'center' }}>
            <h1 style={{ fontSize: '2rem', marginBottom: '1rem' }}>Page not found</h1>
            <a href="/" style={{ color: '#8b1a1a' }}>Go to homepage</a>
          </div>
        </div>
      </body>
    </html>
  );
}
```

- [ ] **Step 6: Lokalizovat [locale]/not-found.tsx**

`apps/web/app/[locale]/not-found.tsx`:

```tsx
import { useTranslations } from 'next-intl';
import { Link } from '@/i18n/navigation';

export default function NotFound(): React.ReactElement {
  const t = useTranslations('common');
  return (
    <div className="flex min-h-screen items-center justify-center bg-[#f0e6d0] px-4">
      <div className="w-full max-w-lg rounded-lg border border-[#c9b99a] bg-[#faf4e8] p-8 text-center shadow-lg">
        <h1 className="mb-4 font-serif text-3xl font-bold text-[#8b1a1a]">{t('notFound')}</h1>
        <p className="mb-6 font-serif text-[#3d2b1f]">{t('notFoundDescription')}</p>
        <Link
          href="/"
          className="inline-block rounded-md bg-[#8b1a1a] px-6 py-2 font-serif text-white transition-colors hover:bg-[#6d1515]"
        >
          {t('backToHome')}
        </Link>
      </div>
    </div>
  );
}
```

- [ ] **Step 7: Lokalizovat [locale]/error.tsx**

`apps/web/app/[locale]/error.tsx`:

```tsx
'use client';

import { useTranslations } from 'next-intl';

export default function GlobalError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}): React.ReactElement {
  const t = useTranslations('common');
  return (
    <div className="flex min-h-screen items-center justify-center bg-[#f0e6d0] px-4">
      <div className="w-full max-w-lg rounded-lg border border-[#c9b99a] bg-[#faf4e8] p-8 text-center shadow-lg">
        <h1 className="mb-4 font-serif text-3xl font-bold text-[#8b1a1a]">{t('errorTitle')}</h1>
        <p className="mb-6 font-serif text-[#3d2b1f]">{error.message || t('errorDescription')}</p>
        <div className="flex flex-col items-center gap-3">
          <button
            onClick={reset}
            className="rounded-md bg-[#8b1a1a] px-6 py-2 font-serif text-white transition-colors hover:bg-[#6d1515]"
          >
            {t('tryAgain')}
          </button>
          <a href="/" className="font-serif text-sm text-[#3d2b1f] underline hover:text-[#8b1a1a]">
            {t('backToHome')}
          </a>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 8: Typecheck**

Run: `npx turbo typecheck --force`
Mohou se objevit importy — opravit pokud je potřeba (cesty se nezměnily, `@/` aliasy zůstávají).

- [ ] **Step 9: Commit**

```bash
git add apps/web/app/
git commit -m "feat: přesun App Router pod [locale]/ + layout split"
```

---

### Task 4: Middleware — locale detection + auth

**Files:**
- Rewrite: `apps/web/middleware.ts`

- [ ] **Step 1: Přepsat middleware**

`apps/web/middleware.ts`:

```typescript
import createMiddleware from 'next-intl/middleware';
import { NextRequest, NextResponse } from 'next/server';
import { routing } from './i18n/routing';

const intlMiddleware = createMiddleware(routing);

const PUBLIC_PATHS = ['/', '/login', '/forgot-password', '/reset-password', '/verify-email', '/view'];

function isPublicRoute(pathname: string): boolean {
  const localePrefix = new RegExp(`^/(${routing.locales.join('|')})`);
  const withoutLocale = pathname.replace(localePrefix, '') || '/';
  return PUBLIC_PATHS.some(
    (p) => withoutLocale === p || withoutLocale.startsWith(p + '/'),
  );
}

function getLocaleFromPath(pathname: string): string {
  const match = pathname.match(new RegExp(`^/(${routing.locales.join('|')})\\b`));
  return match?.[1] ?? routing.defaultLocale;
}

export function middleware(req: NextRequest): NextResponse {
  if (req.nextUrl.pathname.startsWith('/api/')) {
    return NextResponse.next();
  }

  const response = intlMiddleware(req);

  // If next-intl returned a redirect, return it directly
  if (!response.ok) {
    return response;
  }

  // Auth check for non-public routes
  const pathname = req.nextUrl.pathname;
  if (!isPublicRoute(pathname)) {
    const hasSession =
      req.cookies.has('authjs.session-token') ||
      req.cookies.has('__Secure-authjs.session-token');
    if (!hasSession) {
      const locale = getLocaleFromPath(pathname);
      const loginUrl = new URL(`/${locale}/login`, req.nextUrl.origin);
      loginUrl.searchParams.set('callbackUrl', pathname);
      return NextResponse.redirect(loginUrl);
    }
  }

  return response;
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|icon.svg|hero/|api/images).*)'],
};
```

- [ ] **Step 2: Typecheck**

Run: `npx turbo typecheck --filter=@ai-sedlacek/web --force`

- [ ] **Step 3: Commit**

```bash
git add apps/web/middleware.ts
git commit -m "feat: middleware — next-intl locale detection + auth"
```

---

### Task 5: Lokalizace auth stránek

**Files:**
- Modify: `apps/web/app/[locale]/login/page.tsx`
- Modify: `apps/web/app/[locale]/forgot-password/page.tsx`
- Modify: `apps/web/app/[locale]/reset-password/page.tsx`
- Modify: `apps/web/app/[locale]/verify-email/page.tsx`

- [ ] **Step 1: Lokalizovat login stránku**

V `apps/web/app/[locale]/login/page.tsx`:
1. Přidat `import { useTranslations } from 'next-intl';`
2. Přidat `import { Link } from '@/i18n/navigation';` (nahradit next/link)
3. Na začátek `LoginForm` přidat `const t = useTranslations('auth');`
4. Nahradit všechny hardcoded české texty za `t()` volání:
   - `'Přihlášení'` → `t('loginTitle')`
   - `'Registrace'` → `t('registerTitle')`
   - `'Přihlaste se ke svému účtu'` → `t('loginSubtitle')`
   - `'Vytvořte si účet a začněte číst rukopisy'` → `t('registerSubtitle')`
   - `'Pokračovat přes Google'` → `t('continueWithGoogle')`
   - `'nebo'` → `t('or')`
   - `'Jméno'` → `t('name')`
   - `'Volitelné'` → `t('optional')`
   - `'Email'` → `t('email')`
   - `'Heslo'` → `t('password')`
   - `'Zapomenuté heslo?'` → `t('forgotPassword')`
   - `'Nesprávný email nebo heslo'` → `t('wrongCredentials')`
   - `'Email není ověřen.'` → `t('emailNotVerified')`
   - `'Odeslat ověřovací email znovu'` → `t('resendVerification')`
   - `'Registrace selhala'` → `t('registerFailed')`
   - `'Počkejte…'` → `t('pleaseWait')`
   - `'Přihlásit se'` → `t('login')`
   - `'Zaregistrovat se'` → `t('register')`
   - `'Nemáte účet?'` → `t('noAccount')`
   - `'Zaregistrujte se'` → `t('signUp')`
   - `'Máte účet?'` → `t('haveAccount')`
   - `'Přihlaste se'` → `t('signIn')`
   - `'Zpět na hlavní stránku'` → `t('backToHome')`
   - Banner texty: `'Email úspěšně ověřen...'` → `t('emailVerified')`
   - Error bannery: `'Ověřovací odkaz je neplatný...'` → `t('invalidToken')`, `t('missingToken')`
5. Zajistit, že `router.push('/verify-email')` používá locale prefix — nahradit `useRouter` z next/navigation za `useRouter` z `@/i18n/navigation`

- [ ] **Step 2: Lokalizovat forgot-password**

V `apps/web/app/[locale]/forgot-password/page.tsx`:
1. Přidat `useTranslations('auth')`
2. Nahradit české texty za `t()` volání
3. Nahradit `Link` z next/link za `Link` z `@/i18n/navigation`

- [ ] **Step 3: Lokalizovat reset-password**

V `apps/web/app/[locale]/reset-password/page.tsx`:
1. Přidat `useTranslations('auth')`
2. Nahradit české texty za `t()` volání
3. Nahradit `Link` z next/link za `Link` z `@/i18n/navigation`

- [ ] **Step 4: Lokalizovat verify-email**

V `apps/web/app/[locale]/verify-email/page.tsx`:
1. Přidat `useTranslations('auth')`
2. Nahradit české texty za `t()` volání
3. Nahradit `Link` za `Link` z `@/i18n/navigation`

- [ ] **Step 5: Typecheck**

Run: `npx turbo typecheck --filter=@ai-sedlacek/web --force`

- [ ] **Step 6: Commit**

```bash
git add apps/web/app/\[locale\]/login/ apps/web/app/\[locale\]/forgot-password/ apps/web/app/\[locale\]/reset-password/ apps/web/app/\[locale\]/verify-email/
git commit -m "feat: lokalizace auth stránek (login, forgot/reset password, verify email)"
```

---

### Task 6: Lokalizace landing page a public view

**Files:**
- Modify: `apps/web/app/[locale]/(public)/page.tsx`
- Modify: `apps/web/app/[locale]/view/[slug]/page.tsx`
- Modify: `apps/web/app/[locale]/view/[slug]/[pageId]/page.tsx`
- Modify: `apps/web/app/[locale]/view/[slug]/not-found.tsx`

- [ ] **Step 1: Lokalizovat landing page**

V `apps/web/app/[locale]/(public)/page.tsx`:
1. Přidat `import { getTranslations } from 'next-intl/server';`
2. Na začátek server component přidat `const t = await getTranslations('landing');`
3. Nahradit všechny hardcoded texty za `t()` volání
4. Nahradit `Link` z next/link za `Link` z `@/i18n/navigation`

- [ ] **Step 2: Lokalizovat view stránky**

V `apps/web/app/[locale]/view/[slug]/page.tsx` a `[pageId]/page.tsx`:
1. Použít `getTranslations('view')` pro server parts
2. Nahradit české texty
3. Aktualizovat metadata na lokalizované

- [ ] **Step 3: Lokalizovat view not-found**

V `apps/web/app/[locale]/view/[slug]/not-found.tsx`:
1. Použít `useTranslations('common')`
2. Nahradit české texty

- [ ] **Step 4: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/app/\[locale\]/\(public\)/ apps/web/app/\[locale\]/view/
git commit -m "feat: lokalizace landing page a public view stránek"
```

---

### Task 7: Lokalizace workspace a billing stránek

**Files:**
- Modify: `apps/web/app/[locale]/workspace/page.tsx`
- Modify: `apps/web/app/[locale]/workspace/billing/page.tsx`

- [ ] **Step 1: Lokalizovat workspace page**

V `apps/web/app/[locale]/workspace/page.tsx`:
1. Přidat `useTranslations('workspace')` (client component) nebo `getTranslations` (server)
2. Nahradit české texty za `t()` volání
3. Nahradit `Link`/`useRouter` za verze z `@/i18n/navigation`

- [ ] **Step 2: Lokalizovat billing page**

V `apps/web/app/[locale]/workspace/billing/page.tsx`:
1. Přidat `useTranslations('billing')`
2. Nahradit české texty

- [ ] **Step 3: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/app/\[locale\]/workspace/
git commit -m "feat: lokalizace workspace a billing stránek"
```

---

### Task 8: Lokalizace komponent — navigace a layout

**Files:**
- Modify: `apps/web/components/AppShell.tsx`
- Modify: `apps/web/components/Toolbar.tsx`
- Modify: `apps/web/components/Sidebar.tsx`
- Modify: `apps/web/components/Breadcrumbs.tsx`
- Modify: `apps/web/components/UserMenu.tsx`
- Modify: `apps/web/components/ContextMenu.tsx`

- [ ] **Step 1: Lokalizovat každou komponentu**

Pro každou komponentu:
1. Přidat `import { useTranslations } from 'next-intl';`
2. Na začátek komponenty přidat `const t = useTranslations('{namespace}');`
3. Nahradit všechny hardcoded texty za `t()` volání
4. Nahradit `Link` z next/link za `Link` z `@/i18n/navigation` kde je použit
5. Nahradit `useRouter` z next/navigation za `useRouter` z `@/i18n/navigation` kde je použit

Namespace mapování:
- AppShell → `common`
- Toolbar → `workspace`
- Sidebar → `workspace`
- Breadcrumbs → `common`
- UserMenu → `common`
- ContextMenu → `workspace`

- [ ] **Step 2: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/components/AppShell.tsx apps/web/components/Toolbar.tsx apps/web/components/Sidebar.tsx apps/web/components/Breadcrumbs.tsx apps/web/components/UserMenu.tsx apps/web/components/ContextMenu.tsx
git commit -m "feat: lokalizace navigačních a layout komponent"
```

---

### Task 9: Lokalizace komponent — dokumenty

**Files:**
- Modify: `apps/web/components/DocumentPanel.tsx`
- Modify: `apps/web/components/ResultViewer.tsx`
- Modify: `apps/web/components/PublicResultViewer.tsx`
- Modify: `apps/web/components/VersionHistory.tsx`
- Modify: `apps/web/components/DocumentChat.tsx`
- Modify: `apps/web/components/MarkdownEditor.tsx`
- Modify: `apps/web/components/ImageZoom.tsx`

- [ ] **Step 1: Lokalizovat každou komponentu**

Stejný postup jako Task 8. Namespace: `document`.

- [ ] **Step 2: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/components/DocumentPanel.tsx apps/web/components/ResultViewer.tsx apps/web/components/PublicResultViewer.tsx apps/web/components/VersionHistory.tsx apps/web/components/DocumentChat.tsx apps/web/components/MarkdownEditor.tsx apps/web/components/ImageZoom.tsx
git commit -m "feat: lokalizace dokumentových komponent"
```

---

### Task 10: Lokalizace komponent — soubory a kolekce

**Files:**
- Modify: `apps/web/components/FileGrid.tsx`
- Modify: `apps/web/components/FileList.tsx`
- Modify: `apps/web/components/FileUploadZone.tsx`
- Modify: `apps/web/components/ImportDialog.tsx`
- Modify: `apps/web/components/CreateCollectionDialog.tsx`
- Modify: `apps/web/components/CreateWorkspaceDialog.tsx`
- Modify: `apps/web/components/ShareDialog.tsx`
- Modify: `apps/web/components/CollectionMetadataEditor.tsx`
- Modify: `apps/web/components/CollectionContextDialog.tsx`
- Modify: `apps/web/components/CollectionContextCard.tsx`
- Modify: `apps/web/components/HeroCarousel.tsx`
- Modify: `apps/web/components/NavArrow.tsx`
- Modify: `apps/web/components/PublicPageLayout.tsx`

- [ ] **Step 1: Lokalizovat každou komponentu**

Stejný postup jako Task 8. Namespace: `workspace` pro soubory, `collection` pro kolekce. HeroCarousel → `landing`, NavArrow/PublicPageLayout → `view`.

Také aktualizovat `Link` importy na `@/i18n/navigation` v NavArrow a PublicPageLayout.

- [ ] **Step 2: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/components/FileGrid.tsx apps/web/components/FileList.tsx apps/web/components/FileUploadZone.tsx apps/web/components/ImportDialog.tsx apps/web/components/CreateCollectionDialog.tsx apps/web/components/CreateWorkspaceDialog.tsx apps/web/components/ShareDialog.tsx apps/web/components/CollectionMetadataEditor.tsx apps/web/components/CollectionContextDialog.tsx apps/web/components/CollectionContextCard.tsx
git commit -m "feat: lokalizace souborových a kolekčních komponent"
```

---

### Task 11: API locale helper a lokalizace API routes

**Files:**
- Create: `apps/web/lib/infrastructure/api-locale.ts`
- Modify: všechny API routes s českými hláškami

- [ ] **Step 1: Vytvořit API locale helper**

`apps/web/lib/infrastructure/api-locale.ts`:

```typescript
import { getTranslations } from 'next-intl/server';
import { routing } from '@/i18n/routing';

export function getLocaleFromRequest(request: Request): string {
  // 1. Explicit header from frontend
  const header = request.headers.get('x-locale');
  if (header && routing.locales.includes(header as (typeof routing.locales)[number])) {
    return header;
  }
  // 2. Fallback
  return routing.defaultLocale;
}

export async function getApiTranslations(request: Request, namespace: string) {
  const locale = getLocaleFromRequest(request);
  return getTranslations({ locale, namespace });
}
```

- [ ] **Step 2: Lokalizovat auth API routes**

Pro každý soubor v `apps/web/app/api/auth/`:
1. Importovat `getApiTranslations` z `@/lib/infrastructure/api-locale`
2. Na začátek handleru: `const t = await getApiTranslations(request, 'errors');`
3. Nahradit české error hlášky za `t()` volání

Soubory: `register/route.ts`, `forgot-password/route.ts`, `reset-password/route.ts`, `send-verification/route.ts`, `verify-email/route.ts`, `check-verification/route.ts`

- [ ] **Step 3: Lokalizovat ostatní API routes**

Stejný postup pro: `pages/upload/route.ts`, `pages/import-url/route.ts`, `pages/process/route.ts`, `collections/route.ts`, `documents/[id]/route.ts`, `workspaces/route.ts`, `billing/checkout/route.ts`, `user/stats/route.ts`, atd.

- [ ] **Step 4: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/lib/infrastructure/api-locale.ts apps/web/app/api/
git commit -m "feat: lokalizace API routes — chybové hlášky podle locale"
```

---

### Task 12: Email šablony — locale parametr

**Files:**
- Modify: `packages/shared/src/domain/email.ts`
- Modify: `apps/web/lib/adapters/email/resend-email.ts`
- Modify: `apps/web/lib/adapters/email/console-email.ts`
- Modify: `apps/web/lib/infrastructure/verification.ts`
- Modify: `apps/web/app/api/auth/forgot-password/route.ts`

- [ ] **Step 1: Přidat locale do IEmailProvider**

V `packages/shared/src/domain/email.ts`:

```typescript
export interface IEmailProvider {
  sendPasswordReset(email: string, resetUrl: string, locale: string): Promise<void>;
  sendVerification(email: string, verifyUrl: string, locale: string): Promise<void>;
}
```

- [ ] **Step 2: Aktualizovat ResendEmailProvider**

V `apps/web/lib/adapters/email/resend-email.ts`:
1. Obě metody přijmou `locale: string` parametr
2. Vytvořit lokalizované HTML šablony — na základě locale zvolit texty:

```typescript
import en from '../../../messages/en.json';
import cs from '../../../messages/cs.json';

const messages: Record<string, typeof en> = { en, cs };

function getEmailMessages(locale: string) {
  return messages[locale] ?? messages['en'];
}

async sendVerification(email: string, verifyUrl: string, locale: string): Promise<void> {
  const t = getEmailMessages(locale).emails.verification;
  const { error } = await this.client.emails.send({
    from: this.from,
    to: email,
    subject: t.subject,
    html: verificationHtml(verifyUrl, t),
  });
  // ...
}
```

Aktualizovat `verificationHtml` a `passwordResetHtml` aby přijímaly texty jako parametr.

- [ ] **Step 3: Aktualizovat ConsoleEmailProvider**

Přidat `locale` parametr do obou metod. Logovat locale.

- [ ] **Step 4: Aktualizovat callery**

V `apps/web/lib/infrastructure/verification.ts`:
- `sendVerificationEmail(email, locale)` — přidat `locale` parametr
- Předat do `getEmailProvider().sendVerification(email, verifyUrl, locale)`

V `apps/web/app/api/auth/forgot-password/route.ts`:
- Získat locale z requestu
- Předat do `sendPasswordReset(email, resetUrl, locale)`

V `apps/web/app/api/auth/register/route.ts`:
- Přidat locale parametr do `sendVerificationEmail(email, locale)`

V `apps/web/app/api/auth/send-verification/route.ts`:
- Přidat locale parametr do `sendVerificationEmail(email, locale)`

- [ ] **Step 5: Typecheck + Commit**

```bash
npx turbo typecheck --force
git add packages/shared/src/domain/email.ts apps/web/lib/adapters/email/ apps/web/lib/infrastructure/verification.ts apps/web/app/api/auth/
git commit -m "feat: lokalizované email šablony — locale parametr v IEmailProvider"
```

---

### Task 13: User.locale — DB migrace a API endpoint

**Files:**
- Modify: `apps/web/prisma/schema.prisma`
- Create: `apps/web/prisma/migrations/XXXXXX_add_user_locale/migration.sql`
- Create: `apps/web/app/api/user/locale/route.ts`

- [ ] **Step 1: Přidat locale do Prisma schema**

V `apps/web/prisma/schema.prisma`, v modelu `User` přidat:

```prisma
locale  String?
```

- [ ] **Step 2: Vytvořit migraci**

```bash
npx prisma migrate dev --name add_user_locale --schema=apps/web/prisma/schema.prisma
```

Toto automaticky vygeneruje migraci a aplikuje ji na lokální DB. Ověřit, že vygenerovaný SQL obsahuje `ALTER TABLE "User" ADD COLUMN "locale" TEXT`.

- [ ] **Step 3: Vytvořit API endpoint**

`apps/web/app/api/user/locale/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/infrastructure/db';
import { requireUserId } from '@/lib/auth';
import { routing } from '@/i18n/routing';

export async function PATCH(request: NextRequest): Promise<NextResponse> {
  let userId: string;
  try {
    userId = await requireUserId();
  } catch {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  let body: unknown;
  try {
    body = await request.json();
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 });
  }

  const { locale } = (body as { locale?: string }) ?? {};

  if (!locale || !routing.locales.includes(locale as (typeof routing.locales)[number])) {
    return NextResponse.json(
      { error: `Invalid locale. Supported: ${routing.locales.join(', ')}` },
      { status: 400 },
    );
  }

  await prisma.user.update({
    where: { id: userId },
    data: { locale },
  });

  const response = NextResponse.json({ locale });
  response.cookies.set('NEXT_LOCALE', locale, {
    path: '/',
    maxAge: 365 * 24 * 60 * 60,
    sameSite: 'lax',
  });
  return response;
}
```

- [ ] **Step 4: Typecheck + Commit**

```bash
npx turbo typecheck --force
git add apps/web/prisma/ apps/web/app/api/user/locale/
git commit -m "feat: User.locale — DB migrace + PATCH API endpoint"
```

---

### Task 14: NEXT_LOCALE cookie při přihlášení

**Files:**
- Modify: `apps/web/lib/auth.ts`

- [ ] **Step 1: Přidat GET handler pro user locale**

V `apps/web/app/api/user/locale/route.ts` přidat GET handler (vedle existujícího PATCH z Task 13):

```typescript
export async function GET(): Promise<NextResponse> {
  let userId: string;
  try {
    userId = await requireUserId();
  } catch {
    return NextResponse.json({ locale: null });
  }

  const user = await prisma.user.findUnique({
    where: { id: userId },
    select: { locale: true },
  });

  return NextResponse.json({ locale: user?.locale ?? null });
}
```

- [ ] **Step 2: Nastavit NEXT_LOCALE cookie po přihlášení (client-side)**

V login stránce (`apps/web/app/[locale]/login/page.tsx`), po úspěšném `signIn`:

```typescript
// After successful signIn, before redirect
const localeRes = await fetch('/api/user/locale');
const { locale: userLocale } = (await localeRes.json()) as { locale: string | null };
if (userLocale) {
  document.cookie = `NEXT_LOCALE=${userLocale};path=/;max-age=${365 * 24 * 60 * 60};samesite=lax`;
}
router.push(callbackUrl);
```

Toto je jednodušší než JWT-based přístup — nevyžaduje změny v auth.ts callbacks.

- [ ] **Step 2: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/lib/auth.ts apps/web/app/api/user/locale/ apps/web/app/\[locale\]/login/
git commit -m "feat: NEXT_LOCALE cookie při přihlášení z user preference"
```

---

### Task 15: LocaleSwitcher komponenta

**Files:**
- Create: `apps/web/components/LocaleSwitcher.tsx`
- Modify: `apps/web/components/AppShell.tsx` nebo `UserMenu.tsx`

- [ ] **Step 1: Vytvořit LocaleSwitcher**

`apps/web/components/LocaleSwitcher.tsx`:

```tsx
'use client';

import { useLocale } from 'next-intl';
import { useRouter, usePathname } from '@/i18n/navigation';
import { routing } from '@/i18n/routing';

const LOCALE_LABELS: Record<string, string> = {
  en: 'EN',
  cs: 'CZ',
};

export default function LocaleSwitcher(): React.JSX.Element {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const handleChange = async (newLocale: string): Promise<void> => {
    // If user is logged in, save preference
    try {
      await fetch('/api/user/locale', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ locale: newLocale }),
      });
    } catch {
      // Not logged in or error — just redirect, cookie set by PATCH response
    }
    router.replace(pathname, { locale: newLocale });
  };

  return (
    <div className="flex items-center gap-1">
      {routing.locales.map((l) => (
        <button
          key={l}
          onClick={() => void handleChange(l)}
          className={`rounded px-2 py-1 font-serif text-xs font-medium transition-colors ${
            l === locale
              ? 'bg-[#8b1a1a] text-white'
              : 'text-[#7a6652] hover:bg-[#e8dcc6] hover:text-[#3d2b1f]'
          }`}
        >
          {LOCALE_LABELS[l] ?? l.toUpperCase()}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: Přidat do navigace**

V `apps/web/components/UserMenu.tsx` nebo `AppShell.tsx`, přidat `<LocaleSwitcher />` do horní lišty vedle user menu.

Pro nepřihlášené uživatele: přidat do login stránky (např. v horním rohu).

- [ ] **Step 3: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/components/LocaleSwitcher.tsx apps/web/components/UserMenu.tsx apps/web/app/\[locale\]/login/
git commit -m "feat: LocaleSwitcher komponenta — přepínání CZ/EN"
```

---

### Task 16: Frontend fetch wrapper — X-Locale header

**Files:**
- Create: `apps/web/lib/infrastructure/api-client.ts`
- Modify: komponenty/stránky volající fetch

- [ ] **Step 1: Vytvořit fetch wrapper**

`apps/web/lib/infrastructure/api-client.ts`:

```typescript
/**
 * Fetch wrapper that automatically adds X-Locale header.
 * Use instead of raw fetch() for API calls from client components.
 */
export function apiFetch(input: RequestInfo | URL, init?: RequestInit): Promise<Response> {
  const locale = typeof window !== 'undefined'
    ? window.location.pathname.match(/^\/(en|cs)\b/)?.[1] ?? 'en'
    : 'en';

  const headers = new Headers(init?.headers);
  if (!headers.has('x-locale')) {
    headers.set('x-locale', locale);
  }

  return fetch(input, { ...init, headers });
}
```

- [ ] **Step 2: Nahradit fetch() za apiFetch() v client komponentách**

Projít komponenty volající `fetch('/api/...')` a nahradit za `apiFetch('/api/...')`. Klíčové soubory:
- Workspace page (process, sort, delete, etc.)
- DocumentPanel (retranslate)
- FileUploadZone, ImportDialog
- CollectionMetadataEditor, CollectionContextDialog
- Login, verify-email, forgot-password, reset-password (auth fetch calls)
- Billing page

- [ ] **Step 3: Typecheck + Commit**

```bash
npx turbo typecheck --filter=@ai-sedlacek/web --force
git add apps/web/lib/infrastructure/api-client.ts apps/web/components/ apps/web/app/
git commit -m "feat: apiFetch wrapper — automatický X-Locale header pro API volání"
```

---

### Task 17: Verification sweep — zbývající next/link a next/navigation importy

Tasks 5-10 už nahrazují importy v jednotlivých souborech. Tento task je **kontrolní sweep** pro zachycení čehokoliv, co bylo přehlédnuto.

**Files:**
- Potenciálně libovolný soubor v `apps/web/app/[locale]/` a `apps/web/components/`

- [ ] **Step 1: Vyhledat zbývající next/link importy**

```bash
grep -r "from 'next/link'" apps/web/app/\[locale\]/ apps/web/components/ --include="*.tsx" --include="*.ts" -l
```

Pro každý nalezený soubor: nahradit `import Link from 'next/link'` → `import { Link } from '@/i18n/navigation'`.

- [ ] **Step 2: Vyhledat zbývající next/navigation importy**

```bash
grep -r "from 'next/navigation'" apps/web/app/\[locale\]/ apps/web/components/ --include="*.tsx" --include="*.ts" -l
```

Pro `useRouter` a `usePathname` nahradit za `@/i18n/navigation`. Ponechat `useSearchParams` z `next/navigation` (next-intl ho neposkytuje).

- [ ] **Step 3: Zkontrolovat sitemap.ts**

Soubor `apps/web/app/sitemap.ts` zůstává na root úrovni. Pokud generuje URLs, přidat locale prefix pro každý podporovaný jazyk.

- [ ] **Step 4: Typecheck + Lint + Commit**

```bash
npx turbo typecheck --force && npx turbo lint --force
git add apps/web/
git commit -m "fix: sweep zbývajících next/link a next/navigation importů"
```

---

### Task 18: Finální typecheck, lint, a manuální test

- [ ] **Step 1: Typecheck celého projektu**

Run: `npx turbo typecheck --force`
Expected: 0 errors

- [ ] **Step 2: Lint**

Run: `npx turbo lint --force`
Expected: 0 errors

- [ ] **Step 3: Format check**

Run: `npx turbo format:check`

- [ ] **Step 4: Spustit dev server a manuální test**

```bash
npx turbo dev
```

Test:
1. Otevřít `http://localhost:3003` → redirect na `/en` (nebo `/cs` podle browseru)
2. Přepnout na `/cs` přes LocaleSwitcher → české texty
3. Registrace → verify-email stránka v aktuálním locale
4. Login → workspace v aktuálním locale
5. Přepnout jazyk → uloží preference, stránka se přeloží
6. API chyby zobrazí se ve správném jazyce
7. Sdílený link `/view/abc` → redirect na `/en/view/abc`

- [ ] **Step 5: Spustit migraci**

```bash
npx prisma migrate deploy --schema=apps/web/prisma/schema.prisma
```
